

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> 💻 Crud function </title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .product-image {
            width: 100%;
            height: auto;
        }
        .card {
            transition: transform 0.1s, box-shadow 0.2s;
        }
        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 8px rgb(111, 175, 218);
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">DISPLAYING ALL PRODUCTS</h1>
        <div class="row justify-content-center">
            <% for(let item of products){ %>
                <div class="col-sm-6 col-md-4 col-lg-3 mb-4 d-flex">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= item.img %>" alt="<%= item.name %>" class="card-img-top product-image">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title"><%= item.name %></h5>
                            <h6 class="card-text"><%= item.price %></h6>
                            <p class="card-text"><%= item.desc %></p>
                            <div class="mt-auto d-flex justify-content-between">
                                <a href="/products/<%= item._id %>" class="btn btn-primary mr-2">Show</a>
                                <a href="/products/<%= item._id %>/edit" class="btn btn-secondary mr-2">Edit</a>
                                <form action="/products/<%= item._id %>?_method=DELETE" method="POST" class="d-inline">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    
</body>
</html>
